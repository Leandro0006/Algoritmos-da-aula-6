#include <iostream>
#include <string>
using namespace std;

int main() {
    string a, b;

    cout << "Digite a primeira string: ";
    getline(cin, a);  

    cout << "Digite a segunda string: ";
    getline(cin, b);

    cout << "a == b: " << boolalpha << (a == b) << endl;
    cout << "a < b: " << boolalpha << (a.compare(b) < 0) << endl;
    cout << "b < a: " << boolalpha << (b.compare(a) < 0) << endl;

    return 0;
}
